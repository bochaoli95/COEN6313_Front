version: "3.9"

services:
  vue-frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Backend API URL - directly accessible from container
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://20.151.88.17:8006/api}
    container_name: resume-frontend
    ports:
      - "3000:3000"
    restart: always